[project]
name = "fastapi-uv-sample"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi==0.115.6",
    "uvicorn[standard]==0.32.1",
]

[dependency-groups]
dev = [
    "pytest==8.3.4",
    "httpx==0.27.2",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
